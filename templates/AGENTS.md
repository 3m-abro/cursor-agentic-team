# Agentic Team project instructions

Use the installed `team-orchestrator` skill to classify intent into DEV, DESIGN,
MARKETING, SOCIAL, FINANCE, BIZ, or LEGAL before implementation. In Cursor the
always-on `ceo-router` rule provides the same routing. Invoke the corresponding
43 department skills by name, retaining the installed plugin namespace if needed.
If the skill/plugin is not available, report that setup is missing; do not invent it.

Use the host's available tools and explicitly report missing dependencies. Respect
this project's constraints. Read `docs/ai-collab/HANDOVER.md` when present. Preserve
existing collaboration files; maintain decision and test evidence for substantial work.
Never claim human diff review. Escalate HIGH legal risk to counsel before relying on it.
